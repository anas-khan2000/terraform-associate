1. Create a separate directory for modules
2. Inside the directory creater server.tf file and provided an instance code
3. Refernced that server.tf inside the root(parent) module.
4. Reuse the module to create various instances